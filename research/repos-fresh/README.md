# repos-fresh/ —— fresh 波語料(gitignored)

2026-09-02 收集;選單/理由/commit 釘在 `../clone-manifest-fresh-wave.json`(進版控)。
⚠️ untrusted clone,只做靜態分析,**絕不執行任何檔案**(Iron Rule 7)。
重建:依 manifest 的 commit 做完整 clone(shallow 只拿得到上游 HEAD)。
