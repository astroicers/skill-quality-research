# 回灌:L-004 ← readme-reviewer R-004 已驗證結構(2026-09-02)

> rubric 3.2.1 → **3.3.0**、工具 2.2.1 → **2.3.0**(前置衛生 PR 見 3.2.1/2.2.1)。
> 觸發:**獨立第三方證據**——姊妹專案的兩輪盲判(判讀者不是本專案的任何審查者)。

## 為什麼可以不等 misjudgment 批次門檻

處理紀律的門檻擋的是**反射性修補**,不是已查證的裁決(先例:CLAUDE.md 記載的
2026-08-27 收尾清空,「查證在批次期間已做完」)。本案的查證由姊妹專案完成:

- R-004 的形**取自本專案 L-004**(κ=0.400 的那條;數字在量測紀錄,不再入條文)
- 姊妹第一輪盲判(3 位不知情判讀者)量出 R-004 兩兩一致 **69.4%**、
  且同格出現取值域三個值;批次 #2 寫入五結構;第二輪盲判(另 3 位)
  **69.4% → 77.8%,預先登記的分裂格 7/8 三方一致命中**
- 全文:readme-reviewer `reviews/2026-09-02-blind-rejudge-020.md` 與
  `2026-09-02-blind-rejudge-030.md`(統計皆由該 repo 腳本斷言並掛 CI)

這符合本專案 doctrine 的「獨立第三方指出」驅動;misjudgments.md 補一行偏離說明。

## 移植對映(R-004 結構 → L-004 語境改寫)

| R-004 結構 | L-004 落地 | 語境差異 |
|---|---|---|
| scope_of_perishable | 同名新增 | 易腐清單改 skill 語境(第三方工具版本/路徑/預設值、未附引用的領域事實、本地產物結構——沿用 exemption 既有的單一提問);「範例 pin 不算」改寫為「範例樣張字面不算」 |
| statement_test | 同名新增 | R-004 測「陳述的排除力」;L-004 測「**機制的攔截力**」(對憑記憶編造)——因為 L-004 的『陳述』本來就是 anti-hallucination 機制。**單純來源標註仍算弱形式**(不推翻既有強弱梯度) |
| 五序 decision_order + 序號必記 | 三序 → 五序 | 新增序 2(機制在但載重裸露/已腐→mixed——**原三序下這格被序 1 吃成 good**,是移植中唯一會改變既有判定的格)與序 3(無機制但機械同步→mixed,封頂 rationale 一行);序 1 空缺連言明定 |
| equivalent_forms | 同名新增 | 裸外鏈不算同步;「先查該頁再答」的指示語歸 statement_test(機制),不是同步 |
| craft_value_mapping | 新增於 craft_llm 組前 | 四維共用;L-004 例外走自己的序(照 readme 的例外句式) |

**續用不動**:pass_criteria 的強弱三級、collection_aggregation、exemption 的單一提問
(它變成 scope 的判別法)、evidence_strength/refs、mechanism。

## 帶縫移植(姊妹已知未解,本版不假裝修好)

繼承的縫,登記在 readme-reviewer misjudgments **批次 #3**(蓄積中):

1. 載重統計的判別法(歷史完成式 / 社會證明數字 / 零元定價)——三位判讀者各自立法
2. 「宣稱即陳述」交疊(同一句過 statement_test 又是裸露宣稱)——姊妹 R2 預登記唯一未中格
3. badge 承載易腐的歸屬(scope 與 equivalent_forms 的拉扯)

skill 側真實使用撞到時,依誤判紀律入本專案 `research/misjudgments.md`,**不重複帳本**。

## 介面凍結驗證(零 ASP 變更)

G5 合約(ASP `pipeline.md` evaluate_G5 消費面)逐項比對:

| 合約面 | 本版動了嗎 |
|---|---|
| `lint.hygiene[].severity ∈ {error,warning,info}` / `pass` | 否(lint 未動判定碼) |
| `security[].polarity / confidence` | 否 |
| `tier_benchmark_packaging` / `_score` | 否 |
| craft rollup 取值域(approved / approved-with-notes / needs-revision) | 否 |
| CLI `--changed-files --json` | 否 |

`rule-registry.yaml` 的 `source:` 為出處字串(observed_by: manual),不需動。

## 驗證

- 九項本地檢查全綠(研究管線 5 支 + lint selftest + stdlib + parser agreement + evals)
- 正本/出貨副本 `diff -q` 兩檔一致(CI 亦有守衛)
- 前置 PR(2.2.1)的五道新守衛 5/5 突變轉紅在案
- ⚠️ **本版沒有自己的盲判驗證**——五結構的效度證據全部來自姊妹語料(README),
  skill 語料上的效度**未量**。下一步的 friction-only 回歸(已核准協定,
  只收筆記不算數字)就是為此;真正的裁決仍是真實使用。

## 對既有判定的影響(誠實聲明)

序 2 是唯一會**改變既有判定**的新格:舊三序下「機制在 + 載重裸露」判 good,
新五序判 mixed。既有 41 個歷史判讀中有多少落此格**未重判**(歷史判讀是舊條文的產物,
留作紀錄;重判會是作者錨定,姊妹實測過那個偏誤)。實際影響由此後的真實使用顯現。
