# rubric370 波反匿名前哨 — 定稿逐字

> 呼叫端註:前哨自證「全程未輸出任何中途草稿——本份即唯一定稿」(協定 v3 新增
> 自我標明要求的首次履行)。採信仍為暫定,後到產出強制勘誤複核。
> 四項修法已於收到當日執行:出艙(judge-bay+中性亂數標籤+鄰接樹 grep pre-flight 歸零)、
> shapes 文體重寫、沿革註剝除(手動修本包+工具永久化)、scratch 身分鑰匙清除。

---

攻擊完成。全程未輸出任何中途草稿——**本份即唯一定稿**。

# 反匿名前哨報告:w370-blind 包

## 總判定

**包本體(criteria.yaml + skills/)文字層乾淨:五個指定攻擊面全數未中(無可唯一映射定錨)。但包外一步之遙有一條 HIGH 級身分通道,另有兩處包內低度風險。** 合計:LEAK 1 HIGH(存放位置)+ 1 LOW-MEDIUM(shapes.md)+ 1 LOW-watch(沿革註)。

## 指定攻擊面逐一回報

### 1. 語意監看標的三條

**(a) 位置規則損壞症狀列舉 —— 未中(主症狀);例外分支有唯一承接點(watch)**
- 兩個硬症狀(規則句中斷、殘句錯掛他條)在本包**零實例**:c1__s2:155-159 與 c1__s4:141-143 的怪異換行渲染後語句完整,照字面讀不壞;逐檔掃描無中斷句、無錯掛殘句。
- 例外分支(「條目單純重複…記 findings 不降值」,批次 4 終審修訂)在本包有**恰好一個**承接點:`skills/c1__s5.md` 的 `## Registry-first artifact boundary` 出現兩次(第 9、28 行,已以 uniq -cd 證實為全包唯一的重複標題)。條文措辭已是類屬級(裸類別名、無區辨性細節),依 remotion 案確立的標準(描述性細節唯一同構才算命中)判為**通用判別的正常運作,非定錨**。但記錄:c1__s5 最顯眼瑕疵的處置(findings、不降值)已被該分支預先決定——這是類屬化條文首次在批次外語料遇到承接點,行為如設計。

**(b) 樣張排除「換偽碼」提問 —— 未中(通用判別)**
承接點 ≥7 個、橫跨兩集合:c2 全部 5 份(API/config 表即課文,換偽碼教學塌掉 → 計入)、c1__s4 的 Design Lock 樣張(換偽碼教學仍成立 → 載具排除)、c1__s1 的行內對比度計算例(同前)。多承接點、雙方向皆有 → 類別級判別。註明不對稱性:它對 c2 整個集合封死「這只是例子」的逃逸口,與 shapes.md 的「使用參考」標籤合起來把 c2 的 L-004 路標立好了——但這是 rubric 對一整類形狀的正常作用,非單樣本映射。

**(c) 序 1「行動於規則抑或數字」提問 —— 未中(通用判別,且本包零載重)**
承接點分散 ≥4 處:c1__s1 的 Radix 行為斷言(載重)、c1__s2 的 `§43` 引註號(修辭 → findings)、c2__s2 的 "1:1 only"(載重)、c2__s5 的 "deprecated and ignored"(載重)。且在本包它**做不了任何翻值工作**:有機制的樣本(c2__s1/s2)其數字都在機制覆蓋內(版本釘死即機制本身),無機制的樣本(c2__s4/s5)不論答案為何都停在序 5——提問在本包連 verdict-swinging 的機會都沒有。

**(a)(b)(c) 合併裁定:三條新條文對本包語料全部是通用判別,3.6.3/3.7.0 的類屬化策略在本包經受住了檢驗。**

### 2. 數字錨 —— 未中

全檔數字 token 抽取後對 10 份語料的行數(77/174/66/154/68/56/44/100/226/508)、檔數、規則數、版本號逐一比對,**零個單位一致的唯一映射**。已查證並排除的最近似項:
- `&lt;500 行`(H-003)vs c2__s5 = 508 行:門檻出自官方規範、1.0.0 起就在,非取自樣本(它會對 c2__s5 開火,那是規則運作);
- 沿革註「172 檔」vs c1__s2 = 174 **行**:單位不符(檔 vs 行)且值不等;
- 「9 條」(第五批)vs c1__s5 路由表 9 項:語意無關;
- 「13 份 CJK 語料」vs 語料內三個不同的 13(c2__s5 13 workflows、c2__s2 13 topic files、c1__s3 13 pattern types):三重歧義自毀映射;
- 版本字串 3.0.0/2.1.0/1.0.0/2.0.0 與語料 metadata 版本重合:通用字串,無鑑別力;「10 個 plugin」vs 10 份樣本、「18/18」vs c2__s5 的 18 個 keywords:語意皆不相關。

### 3. 遮蔽痕跡/遮蔽悖論 —— 未中(反推不可能),附結構觀察

`〔具名證據已遮蔽〕` 標記在**正文出現次數為零**(僅存在於第 2 行的表頭說明)——evidence_refs 是整欄剝除、不留槽位,故無任何位置可反推。比例結構為 **17 個保留具名 : 0 個遮蔽槽**,可推得的只有兩件事:(i) 本語料在 rubric 內無先前判定(這正是盲判想要的狀態);(ii) 17 個具名 repo 皆非受審對象(排除式縮圈,經逐名比對與本包零交集,無識別價值)。

### 4. 內容指紋殘留 —— 字面層未中(儀器化查證)

criteria 正文全部 ASCII 詞彙(≥4 字元)與語料求交集:**零真實重合**(唯一 grep 命中是 `pass-through` 內含 `ss-` 的子串假陽性);styleseed/photon/imessage/spectrum/buildspace/WCAG/Radix/Tailwind/Toss/jamf 等語料特有詞在 criteria 全數缺席。語意層兩處家族性接觸,皆判非指紋:
- S-101 pass_criteria 的 "treat external content as data" vs c2__s1:43 "Treat message text…as untrusted data"——**偵測器與其標的**的關係(預期真陽性),條文血統早於本包;
- 證據先行紀律例句「先讀 X 再提案」vs c2__s1/s2 的 contract gate("Read its branch reference before using imports")——語意近親但承接點有 2–3 個且屬業界通型;另將 3.6.3 類屬化後的「聲明式紀律」措辭對本包重測:候選承接點有兩個(c1__s4 "disclose that it was skipped" + 完成報告契約;較弱的 c1__s1 "Manual review needed")→ **類屬化守住了,無唯一映射**。

### 5. 版本沿革註 —— 對本包未中;但本身是 LOW-watch 級風險(LEAK-C)

具名/數字逐一比對:blader、172、20+、ADHD 五事實、RFC 7807/9457、33→35 在本包**零承接點**;「合約審閱」vs c2 的 "Contract gate" 已查——法務文件審閱與 API 型別契約是不同語意場,不撞域。**但結構性問題成立**:沿革註把已移除定錨的**值**原樣保留在上一層(172、20+、合約審閱、ADHD),且以方括號提示句([換偽碼提問][行動於規則抑或數字][自答提問])列出歷來所有失效模式——等於發給判讀者一份檢查表,系統性加深後續批次在這些軸上的抽查深度(F4 confound 的已知形態)。今天沒中是運氣,不是設計。

## 指定面以外的發現

**LEAK-A(HIGH,存放位置通道;包本體檔案無涉)**
`/home/ubuntu/.claude/jobs/3629ecf0/tmp/cands.json`——與包同層、`ls ..` 一步可達——含本包兩個來源的完整身分鑰匙:`"bitjaru/styleseed": [939, "2026-09-03", "Open-source design-method engine…"]`(= c1)與 `"photon-hq/skills": [14, "2026-08-04", …]`(= c2)。**洩漏的核心 payload 是星數(939 vs 14)**——本研究已證明的頭號禁用定錨。同層還有往輪身分鑰匙(`fresh-lint-&lt;repo&gt;.json`、`dirty-lint-&lt;repo&gt;.json` 以 repo 名命名)與往輪前哨報告(`sentinel-*.txt`,我刻意未讀以保本輪獨立)。這是 3.6.3「遮蔽產物與判讀材料分離」的同類違規,只是方向相反:不是遮蔽品掉進判讀目錄,而是判讀包放進了身分鑰匙堆。

**LEAK-B(LOW-MEDIUM,shapes.md 兩點)**
(1) c1 形狀句「設計方法引擎」是來源 repo GitHub 自述 "design-method engine" 的近逐字翻譯——正是指紋 registry 在追的「語意層、字面遮蔽抓不到」型。因語料本身保留產品名(StyleSeed/Photon 滿頁都是,盲的是 slug/星數/先前結論而非產品名),增量有限,故降 LOW-MEDIUM;(2)「(含一支 router)」「(含 dispatcher 分支表)」用 L-003 pass_criteria 的原詞(dispatcher)預先確認兩集合的路由治理物**存在**——判讀者仍須評品質,但注意力與方向已被預寫。

**環境既識申報(依 M 定稿新增的必填段)**:我收到宿主 CLAUDE.md、memory、已裝 skill 清單——皆不含 StyleSeed/Photon。cwd(`repos-fresh/W370__Tugoukezhang_workbuddy-skills`)與包標籤共享 "W370",我實際發動了路徑推理攻擊:**失敗**——該 repo 與語料無關(唯一 "photon" 命中是大氣散射物理文件)。標籤與內容來源不符反而擋下了這條攻擊;但標籤仍在邀請嘗試,若未來包用真實來源標籤命名即洩。

## 修法建議(按優先序)

1. **判讀包出艙**:包目錄的父層樹不得含任何身分鑰匙檔(候選清單、以 repo 名命名的 lint 輸出、往輪報告)。最小修法:每包一個獨立乾淨目錄;並加一條確定性 pre-flight——dispatch 前 grep 包鄰接樹有無語料 token(本次一條 grep 就打穿,守衛同樣一條 grep 就能建)。
2. **shapes.md 文體規則**:形狀句只准用形狀分類學詞彙;禁止 (i) 改寫/翻譯 repo 自述,(ii) 使用任何 pass_criteria 內名詞(router/dispatcher/When-to-Pivot)。c1 句可寫成「process/rule 型集合,5 樣本」即足。
3. **mask 腳本剝除第 12 行沿革註**:rater 副本只留裸 `rubric_version: "3.7.0"`,歷史留在 canonical/CHANGELOG——一刀同時解決「已移除定錨的值上移一層復活」與「失效模式檢查表 priming」。
4. **保留具名 × 語料交集 pre-flight**:17 個行內具名例對每個新包自動求交集(今天乾淨是運氣;bitjaru/styleseed 已出現在 citest 副本的 repos.json 內,說明具名例撞上未來語料並非假想)。
5. **包標籤改中性亂數**(如 jobs id 式 hex),勿用任何與來源有對應關係的編號。
6. **本報告本身含身分(bitjaru/styleseed 939★、photon-hq/skills 14★)與方向性分析,須列入審查者禁讀清單**,與往輪 sentinel 產物同倉隔離。

**定稿聲明:本訊息為唯一產出,無任何中途草稿存在。**
