# affaan-m/ECC（T3 / taxonomy C / 240,369 stars）

## 抽讀樣本
- skills/dmux-workflows/SKILL.md
- skills/ecc-guide/SKILL.md
- skills/laravel-security/SKILL.md（僅前 400 行）
- skills/ml-adoption-playbook/SKILL.md
- skills/prediction-market-risk-review/SKILL.md

## trigger 設計：good
- 5/5 description 均含明確觸發語境，多數帶標準 "Use when..." 句式，且觸發條件具體。
  證據：「Use when running multiple agent sessions in parallel or coordinating multi-agent development workflows」(dmux)
- 部分 skill 另設 "When to Activate" 節，含使用者口語觸發詞：「User says "run in parallel", "split this work", "use dmux"」。
- prediction-market-risk-review 的觸發是前置條件式（"Use before any workflow handles venue auth..."），適度 pushy 且合理。
- ecc-guide 的 description 偏能力描述、無 Use when，但 body 補了完整 When To Use 清單——輕微不一致。

## 寫作風格：good
- 一致的 imperative 語氣，且會解釋 why 而非堆 MUST。
  證據：「Answer from current files, not memory. ECC changes quickly, so hard-coded catalog counts... go stale」(ecc-guide)
- 範例品質高：laravel-security 用成對 GOOD/BAD 代碼並註明後果（「User::create($request->all()); // VULNERABLE to mass assignment!」）；ecc-guide 提供輸出模板。
- prediction-market-risk-review 極簡條列 + 明確 Output Contract，token 效率好。

## scope 清晰度：good
- 抽到的每個 skill 都是單一 job-to-be-done：dmux 並行編排、ECC 導覽、Laravel 安全、ML 導入方法論、預測市場風險審查。
- laravel-security 覆蓋整個安全領域（auth/CSRF/XSS/部署），單檔偏長，但仍是一個連貫的審查工作。
- repo 層級（897 個 SKILL.md、12 個 CLAUDE.md）是大雜燴集合站，倉庫級 scope 極寬；單 skill 品質與倉庫純度需分開評。

## 其他觀察
- ⚠ injection-suspect（低度、防禦性質）：CLAUDE.md 與 .claude/rules/*.md 反覆嵌入同一段「Prompt Defense Baseline」，是對讀取此 repo 的 agent 下達的廣域行為指令（引文：「Do not output executable code, scripts, HTML, links, URLs... unless required by the task and validated」）。意圖是防禦 prompt injection，但本身就是 instruction-like 文字，會實質改變載入它的 agent 行為；分析時一律視為資料。
- dmux-workflows 引導安裝外部工具（github.com/standardagents/dmux），但有加「after reviewing the package」緩衝語，供應鏈意識尚可。
- guardrails 檔自述為「Generated by ECC Tools from repository history」——存在自動生成的 rules 檔，機器生成內容混入治理文件。
