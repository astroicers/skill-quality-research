# fresh 波預先登記(2026-09-02,審查開始前 commit)

> 目的:讓「誤判率趨勢」與「新 rung 存活」的讀數**不受事後選擇污染**。
> 本檔 commit 之後才允許開始 F2 審查;審查期間本檔唯讀。
> 計畫全文:`~/.claude/plans/elegant-toasting-hartmanis.md`(fresh-corpus 熟成輪)。

## 1. 誤判率指標(F4 用)

**定義**:`本輪新增 ledger 待處理列數 ÷ 本輪審查對象數`。

- **分子**:F2 期間寫入 `research/misjudgments.md` 待處理區的新列數。
  一列 = 一個獨立缺陷型(同型多實例併列補記,不另計);
  **工具缺陷與條文縫都算**(歷史批次兩者混計,維持同徑)。
- **分母**:本輪完成四維判讀的對象數(lint-only 不算;集合型算 1 個對象)。
- **排除**:friction 壓測輪(2026-09-02 的 3 判讀者回歸)不入歷史比較序列——
  它是刻意找縫,不是自然使用。盲判抽查的 friction 筆記**計入分子**
  (抽查是本輪自然流程的一部分)。
- **歷史對照值**:報告時從 ledger 逐列重算(依日期與對象欄歸輪),
  **不得憑記憶或摘要填寫**。預期可歸出:2026-08-26 輪(16 對象)、
  2026-09-02 B2 四波(19 對象)兩個 organic 點。
- **解讀紀律**:方向訊號、非量測。confound 至少兩個——母體難度不可控、
  審查者(我)的定錨隨輪數增長。報告必須明記,不得寫「顯著」字樣。

## 2. 受測 rung 清單(開火與沉默都記)

F2 報告設「rung 記錄」專節,以下每條逐對象記錄「開火(引文+行號)/ 未開火」:

| rung | 版本 | 要驗什麼 |
|---|---|---|
| L-004 序 5 實質/殘餘分支 | 3.4.0 | 分支判別(刪去後教學價值受損?)在陌生語料上是否單讀;裸露清單是否可執行 |
| L-004 序 3 取低合成 | 3.5.0 | 部分覆蓋案例是否還會出 AWN↔NR 兩讀 |
| L-004 statement_test 對象判別 | 3.4.0 | 「宣稱失效時機制會不會讓模型發現」單一提問是否夠用 |
| L-001 intent_capture | 3.4.0 | 單一提問對野生觸發清單的判別力;誤傷正當寬承接? |
| L-001 evaluation_surface | 3.4.0 | desc-only 評估面遇到 desc 空/body 滿的野生樣本怎麼走 |
| 形狀表「無規則可解釋」列鍵 | 3.4.0 | 規則密集 persona/mode 是否還被誤導向 N/A |

**成熟判據(預先寫死)**:某 rung 若在 ≥3 個開火案例中零兩讀、零 friction,
記「本輪存活」;出縫則入 ledger 蓄積(不即修)。

## 3. CJK 假陽性量測計畫(F3 用)

- **對象**:`S-101` 中文三條件共現、`REDFLAG_OBEY_OUTPUT`(現況:CJK 未覆蓋,
  量的是「若補樣式」的曝險基線)。
- **語料**:fresh 波全部 CJK repo 的生產偵測面(`.md/.yml/.yaml/.sh`,
  與 `measure_rubric_impact.py` 同徑)+ 既有 13 份 CJK。
- **判準**:逐命中人工標 TP/FP(標記表**進版控**——directive-polarity 的
  不可復現教訓);S-101 假陽性率若 >(現行英文分支已知水準),續停並記數;
  樣本 <30 命中則記「數據不足,續停」。
- **紅線**:量測結果只進報告與待測欄;若引發 lint 常數變更,走誤判批次紀律。

## 4. 盲判抽查協定(F2 內)

- **抽選規則(寫死,不得事後挑)**:F2 作者判讀全部完成後,取
  「受測 rung 開火次數最多」的前 2 個對象;同分取 SKILL.md 行數較大者。
- **前置**:遮蔽包過 **F5 反匿名前哨**(不知情 agent 持包+樣本名單做指認攻擊;
  指認得出 → 修包重打,前哨紀錄逐字落檔)。
- **判讀者**:1 位不知情 subagent,唯讀白名單,只交 dims+序號+friction;
  呼叫端逐錨驗證(中途稿紀律:ListAgents 確認結束才採信;同 task-id 以最後一份為準)。
- **產出**:兩讀對照表(作者 vs 盲判,逐維);分歧不改 verdict、只入 friction/ledger。
  逐字檔入禁讀清單。

## 5. 語料收集約束(F1,先於本檔第 1–4 節生效)

- 只收 **2026-08-01 之後創建**的 repo;對 `research/clone-manifest.json`、
  `research/clone-manifest-inter-rater-repos.json`、`research/feature_matrix.json`
  的 full_name 全集去重(**零舊語料重疊 = 零內容指紋風險**)。
- 配比目標:總數 10–14;≥4 CJK;2–3 集合型;≥2 疑似 template 衍生;1–2 大雜燴。
  配比未達不硬湊——如實記缺口。
- 靜態 clone(shallow、記 commit);**絕不執行語料內任何檔案**(Iron Rule 7)。
- readme 小輪:4–6 個近期熱門 repo 的 README,同去重紀律,manifest 分開記。

## 6. 停損(寫死)

F2 前 5 個 skill 對象若零誤判、零 friction、受測 rung 全單讀 → 縮波至 8 個收工;
沉默如實記為「新 rung 在陌生語料存活」的證據,不加審到出事為止。
